services:
    personal-website:
        build: .
        container_name: personal-website
        restart: unless-stopped
        networks:
            - web

networks:
    web:
        name: web
        external: true
